# Company Dashboard

## Job category generation
- Add an action on job creation that generates experience and coding categories from the job description.
- The UI should apply the generated categories into the create form for review before saving.
- The API should validate and return structured categories with weights totaling 100 for each category set.
